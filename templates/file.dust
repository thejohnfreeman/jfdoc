{! First line should be a comment or empty. Indent the rest. !}
      <h3>{file}</h3>
      {#tags.author}<h5>{.}</h5>{/tags.author}
      {tags.description|s}

      {?decls}
      <ul>
        {#decls}
        <li>
          <a href="{href}">{name}</a>
        </li>
        {/decls}
      </ul>
      {/decls}
