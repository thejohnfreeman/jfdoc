{! First line should be a comment or empty. Indent the rest. !}
      <h3>{source.file}</h3>
      {#author}<h4>{.}</h4>{/author}
      {description|s}

      {?decls}
      <ul>
        {#decls}
        <li>
          <a href="{href}">{name}</a>
        </li>
        {/decls}
      </ul>
      {/decls}
